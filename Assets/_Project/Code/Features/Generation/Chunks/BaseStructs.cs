

namespace Code.Features.Chunks {
    // Structs & Config
    public struct ChunkIndex { public int x, y; public ChunkIndex(int x,int y){this.x=x;this.y=y;} }
    public struct LocalPos  { public float x, y; public LocalPos(float x,float y){this.x=x;this.y=y;} }
    public struct WorldPos  { public float x, y; public WorldPos(float x,float y){this.x=x;this.y=y;} }

       